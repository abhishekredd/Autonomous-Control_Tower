# Terminal 1: Risk Detector Agent
python -m app.mcp.servers.risk_detector

# Terminal 2: Route Optimizer Agent
python -m app.mcp.servers.route_optimizer

# Terminal 3: Stakeholder Comms Agent
python -m app.mcp.servers.stakeholder_comms

# Or start all MCP agents via Docker
docker-compose up -d mcp-risk-detector mcp-route-optimizer mcp-stakeholder-comms